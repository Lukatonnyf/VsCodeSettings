import type { Config } from 'tailwindcss';

const config: Config = {
  content: [],
  theme: {
    extend: {
      colors: {
grape: "var(--color-bg-primary)",
      },
    },
  },
  plugins: [],
};

export default config;
